<div class="row">
    <div class="col-md-6 mx-auto pt-4">
        {{!-- <a href="/u/{{this.user.username}}/{{this.id}}" class="card-link"> --}}
            <div class="card text-dark shadow" id="post-link" data-id={{this.id}} data-user={{this.user.username}}>
                <div class="card-header">
                    <h3>{{this.title}}</h3>
                    <p class="small">Created {{format_date this.createdAt}} by {{this.user.username}}</p>
                    <p>Created {{format_date this.createdAt}}</p>
                </div>
                </a>
                <div class="d-grid gap-2 d-md-block">
                    <button class="btn btn-primary delete-btn" id={{this.id}} type="button" style=" margin-top: -100px; padding-left: 100px; float: right;">Delete</button>
                    <br>
                    <button class="btn btn-primary edit-btn" id={{this.id}} type="button" style=" margin-top: -100px; padding-left: 100px; float: right;">Edit</button>
                </div>
                <div class="card-body text-truncate">
                    {{this.post_body}}
                </div>

                <div class="card-footer">
                    <span>{{num_of_comments this.comments}}</span>
                </div>
            </div>
        {{!-- </a> --}}
    </div>
</div>

<style>
  .card:hover {
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    transform: scale(1.02);
    transition: all 0.3s ease;
  }
  h3 {
    font-family: 'Amatic SC', cursive;
    {{!-- font-family: 'Macondo', cursive; --}}
  }
    .card-header {
        background: beige;
    }
    .card-body {
        background:blanchedalmond;
    }
    .card-footer {
        background: burlywood;
    }

</style>
