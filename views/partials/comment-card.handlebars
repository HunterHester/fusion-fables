<div class="container">
	<div class="row">
        <div class="col-md-10 mx-auto text-left mt-3 mb-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{user.username}}</h5>
                    <h6 class="card-subtitle mb-2 text-muted">{{comment.comment_body}}</h6>
                    <p class="card-text">{{comment.comment_body}}</p>
                </div>
            </div>
        </div>
    </div>
</div>
